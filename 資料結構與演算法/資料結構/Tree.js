/**
 * 每個節點稱為node
 * 分為parent node(根節點) and children node(子節點)
 * 沒有循環的graph
 * 最上層的節點稱為ROOT
 */

/**
 * 1. Binary Tree(二元樹)
 * 每一個節點最多只能有兩個子節點(left node) and (right node)
 * 一個FULL BINARY TREE的每個節點都有對應的left node and right node且深度一樣
 * Max heap: 最大的值永遠是最上層的節點(root) the root is always the biggest
 */
